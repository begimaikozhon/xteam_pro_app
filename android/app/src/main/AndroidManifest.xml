<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.xteam_pro_app">

    <!-- Разрешения для работы с картами и местоположением -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:label="xteam_pro_app"
        android:icon="@mipmap/ic_launcher"
        android:theme="@style/LaunchTheme"
        android:enableOnBackInvokedCallback="true"
        android:usesCleartextTraffic="true">

        <!-- Ключ API для работы с Google Maps -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDa61VbfNlKfMMRob44Du_hlFfKWFS4EUk" /> <!-- Здесь нужно вставить ваш ключ API Google Maps -->

        <!-- Активность для запуска -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            
            <!-- Указываем тему для активности, которая используется до инициализации Flutter -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"/>
              
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Обязательный блок для Flutter -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <!-- Для обработки текстовых действий -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
